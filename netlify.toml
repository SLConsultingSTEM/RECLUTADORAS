[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Demo / avances: datos mock. Cambiar a "false" y definir VITE_API_URL para API real.
  VITE_USE_MOCK = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
